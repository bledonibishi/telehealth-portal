---
description: Create a GitHub pull request from the current branch into dev, running tests first.
---

Use the Agent tool with `subagent_type: "create-pr"` to create a pull request from the current branch into `dev`. Report back the PR URL it returns.
